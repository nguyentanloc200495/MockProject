@using MockProject.Common
@using CommonData
@using PagedList.Mvc;
@model MockProject.Services.PagedSearchList<MockProject.DataBase.CTPHIEUNHAPXUATKHO>
<div class="table-responsive">
    <div class="m-t-lg">
        <h5 class="m-t-lg">Tìm thấy @Model.TotalItemCount</h5>
    </div>
    <table class="table table-hover table-bordered table-striped">
        <tr>
            <th>#</th>

            <th>
                Mã phiếu
            </th>
            <th>
                Sản phẩm
            </th>
            <th>
                Số lượng
            </th>
            <th>
                Đơn giá
            </th>
        </tr>
        @{ int index = Model.PageSize * (Model.PageNumber - 1) + 1; }
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @(index++)
                </td>

                <td>

                  @item.NhapXuatKhoID

                </td>
                <td>
                    @item.SANPHAM.TenSP
                </td>
                <td>
                    @item.SoLuong
                </td>
                <td>
                   @item.DonGia
                </td>


            </tr>
        }
    </table>
    Trang @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) / @Model.PageCount
    @Html.PagedListPager(Model, page =>
{
    Model.SearchModel.PageIndex = page;
    return Url.Action("ListCTPNX", Model.SearchModel);
},
        PagedListRenderOptions.EnableUnobtrusiveAjaxReplacing(new AjaxCommonOption() { HttpMethod = "POST", UpdateTargetId = "content" }))
</div>


